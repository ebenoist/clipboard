const React = require('react');
const ReactDOM = require('react-dom');
const ClipboardInput = require('./clipboard-input.jsx');

ReactDOM.render(
  <ClipboardInput />,
  document.getElementById('main')
);
